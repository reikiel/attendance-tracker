import React from "react";
import Temp from "../components/Temp";

// TODO: Calendar component, list of dates component

const Home = () => {
    return (
        <div>
            <Temp />
        </div>
    );
};

export default Home;
